/**
 * <p>Пример корректно аннотированного класса с использованием аннотации {@link Two}.</p>
 *
 * <p>Класс помечен аннотацией {@code @Two} со следующими параметрами:
 * <ul>
 *   <li>{@code first = "Laboratory"} — строковое значение, описывающее назначение или контекст класса;</li>
 *   <li>{@code second = 6} — целочисленное значение, например, версия, номер лабораторной работы или идентификатор.</li>
 * </ul>
 * </p>
 *
 * <p>Этот класс используется в качестве позитивного примера при тестировании валидации аннотаций,
 * например в {@link TwoAnnotationValidationTest}.</p>
 *
 * <p>Сам по себе класс не содержит полей или методов и служит исключительно
 * для демонстрации и тестирования метаданных на уровне типа.</p>
 *
 * @see Two
 * @see TwoAnnotationValidationTest
 */
@Two(first = "Laboratory", second = 6)
public class TwoExample {
    // Класс-заглушка для демонстрации корректной аннотации @Two
}
