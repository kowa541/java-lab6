/**
 * <p>Пример класса с <em>некорректной</em> аннотацией {@link Two}.</p>
 *
 * <p>Аннотация содержит недопустимые значения параметров:
 * <ul>
 *   <li>{@code first = ""} — пустая строка, которая может нарушать правила валидации
 *       (например, требование непустого идентификатора);</li>
 *   <li>{@code second = -1} — отрицательное число, в то время как логика приложения
 *       может ожидать неотрицательное значение (например, версию, приоритет или номер).</li>
 * </ul>
 * </p>
 *
 * <p>Этот класс используется исключительно в тестах (например, в
 * {@link TwoAnnotationValidationTest}) для проверки корректной обработки
 * ошибочных конфигураций аннотаций. При попытке валидации такого класса
 * ожидается выброс {@link IllegalArgumentException}.</p>
 *
 * <p>Сам по себе класс не содержит логики и служит исключительно как
 * тестовый случай с нарушением правил метаданных.</p>
 *
 * @see Two
 * @see TwoAnnotationValidationTest
 */
@Two(first = "", second = -1)
public class TwoInvalidExample {
    // Класс-заглушка с намеренно некорректной аннотацией @Two
}
