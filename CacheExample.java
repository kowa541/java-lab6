/**
 * <p>Пример класса, демонстрирующего использование аннотации {@link Cache}.</p>
 *
 * <p>Этот класс помечен аннотацией {@code @Cache} с указанием трёх кешируемых областей:
 * {@code "users"}, {@code "sessions"} и {@code "profiles"}.
 * Может использоваться в системах, где требуется разметка классов для последующей обработки
 * (например, в кеширующих прокси или фреймворках управления данными).</p>
 *
 * <h3>Пример аннотации:</h3>
 * <pre>{@code
 * @Cache({"users", "sessions", "profiles"})
 * public class CacheExample { ... }
 * }</pre>
 *
 * <p>Этот класс не содержит логики и предназначен исключительно для демонстрационных целей.</p>
 *
 * @see Cache
 */
@Cache({"users", "sessions", "profiles"})
public class CacheExample {
    // Класс-заглушка для демонстрации аннотации
}
